import os from "node:os";

// import inquirer from "inquirer";

// const ui = new inquirer.ui.BottomBar();

export const NEW_LINE = os.EOL;

// export function print(...messages: string[]) {
//   messages.forEach((m) => ui.log.write(m));
// }
